name: Check submission for US-RSE'24

on:
  push:

jobs:
  check-submission:
    uses: lbianchi-lbl/notebooks-ci/.github/workflows/check-submission.yml@dd9161a695b343bde8760ce1bc78f0adc4173ea3
    with:
      repository: ${{ github.repository }}
      ref: ${{ github.sha }}
      binder-cache-branch: binder-cache
      build-artifacts: |
        my_doc.html
