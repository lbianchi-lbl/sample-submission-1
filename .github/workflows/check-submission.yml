name: Check submission for US-RSE'24

on:
  push:

jobs:
  check-submission:
    uses: USRSE/notebooks-submissions/.github/workflows/check-submission.yml@04092a8f6eb8523c12dbddd3aa01f45fc8324d10
    with:
      notebook: my_doc.ipynb
      repository: ${{ github.repository }}
      ref: ${{ github.sha }}
      binder-cache-branch: binder-cache
      build-artifacts: |
        my_doc.html
